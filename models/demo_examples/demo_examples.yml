version: 2

models:
  - name: materialization_incremental
    description: demo to show incremental model
    columns:
      - name: customer_key
        tests:
          - unique
          - not_null

  - name: use_variables
    description: demo to show variables
    columns:
      - name: order_item_key
        tests:
          - unique
          - not_null 

  - name: get_area_of_circle
    description: demo to show udfs
  
  - name: dynamic_table
    description: a demo of a dynamic table built on the jaffle shop data 
    columns:
      - name: order_month
        tests: 
            - not_null

        